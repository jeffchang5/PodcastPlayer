<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="io.jeffchang.nasademo.PhotoActivity">
    <item
        android:id="@+id/action_sort_by"
        android:orderInCategory="100"
        android:title="@string/action_sort_by"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/action_rover"
                android:title="@string/rover" />
            <item
                android:id="@+id/earth_date"
                android:title="@string/earth_date" />
            <item
                android:id="@+id/camera_type"
                android:title="@string/camera_type" />
        </menu>

    </item>
</menu>
